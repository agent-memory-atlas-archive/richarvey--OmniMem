# Floors are the versions production runs on; ceilings stop the next major
# arriving in CI and the images unannounced (#37). Lift them deliberately.
# mcp is listed because fastmcp's metadata doesn't declare it.
fastmcp>=4.0.3,<5
mcp>=2.2.0,<3
valkey[hiredis]>=6.0.0
onnxruntime>=1.17.0
tokenizers>=0.15.0
huggingface_hub>=0.25.0
numpy>=1.26.0
anthropic>=0.25.0
python-dotenv>=1.0.0
pydantic>=2.0.0
ulid-py>=1.1.0
dateparser>=1.2.0
